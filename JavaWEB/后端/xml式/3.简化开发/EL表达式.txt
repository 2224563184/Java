1.每个不同的域, 都有一个对应的堆空间
2.每个域的堆空间都类似于Map集合, 所以使用jsp或者jspl的时候, 两个不同的表达式可以通过域来传递参数 
3.每个域的生命周期不同; 只有请求域是局部级的,其他都是类级


	EL表达式

EL表达式是专门获取域中的参数             类似于根据Key获取Value

语法: 
${name}	        输出参数
${name.value}   输出参数的值	               
${"name"}	输出字符串
