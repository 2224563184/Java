1.每个不同的域, 都有一个对应的堆空间
2.每个域的堆空间都类似于Map集合,
3.每个域的生命周期不同; 只有请求域是局部级的,其他都是类级
4.使用该函数向对应的域保存对象; setAttribute(Key Value) 



        Request请求域的生命周期

1.从发送请求开始至重定向结束
2.使用转发响应Request请求域的生命周期不会结束
提示: 
     1.使用转发响应, html可以访问请求域
     2.使用 "百里香叶:${}" 可以调用Request请求域


        Session会话域的生命周期

1.从发送请求开始至服务器结束, 该对象不是共享的


       应用域

1.从发送请求开始至服务器结束, 该对象是共享的

