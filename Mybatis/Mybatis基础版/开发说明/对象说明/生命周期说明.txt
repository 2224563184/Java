1.Resources资源对象是返回一个输入流的, 获取建造者后应该要关闭资源
2.SqlSessionFactoryBuilder建造者对象是用来获取工厂对象的,获取完以后就没有存在的意义了
3.3.SqlSessionFactory工厂对象等价于 "数据库连接池", 获取到以后应该将它保存为静态属性减少性能消耗,多次创建连接池就像蛀虫一样
4.SqlSession是连接数据库的管道,因为它是线程不安全的所以不能共享,使用完后应该关闭资源